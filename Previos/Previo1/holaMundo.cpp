// Primer programa en C++

/* se incluye iostream, que sirve para
poder imprimir texto como salida, como
el hola mundo. */
# include <iostream>

/* Se define el bloque main que 
será la función principal del programa */
int main() {  

    /* Se utiliza std::cout para imprimir 
    el Hola Mundo, estudiantes de ie0217! en la consola, 
    el operador<< tiene la función de enviar datos al std::cout*/
    std::cout << "Hola Mundo, estudiantes de ie0217!\n";
    /* Se utiliza la siguiente línea para probar el funcionamiento
    de git stash*/
    std::cout << "Probando el stash";
    /* se retorna un valor entero de cero finalizando
    la función main */
    return 0;

}