//Archivo donde se definen la clase y sus métodos y atributos

/* Se incluye el archivo .hpp que contiene
las declaraciones de las funciones, clases y atributos*/
# include "clases.hpp" 

/* Se define el funcionamiento del constructor de 
Student*/
Student::Student (double m) { 
    marks = m;

        }
